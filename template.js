const firstName = "Jahangir";
const lastName = "Lucchu";
// const fullName = firstName + lastName;
const result = `${firstName} ${lastName} is a good boy. Now, he is ${
    50 + 50
} years old.`;
console.log(result);

const multiLine = `You are not perfect for me.
I am not perfect for you.
We are not made for each other.`;
console.log(multiLine);
